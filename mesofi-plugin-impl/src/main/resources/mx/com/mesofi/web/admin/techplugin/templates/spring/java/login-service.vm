/*
 * COPYRIGHT. Mesofi ${year}. ALL RIGHTS RESERVED.
 *
 * This software is only to be used for the purpose for which it has been
 * provided. No part of it is to be reproduced, disassembled, transmitted,
 * stored in a retrieval system nor translated in any human or computer
 * language in any way or for any other purposes whatsoever without the
 * prior written consent of Mesofi.
 */
package ${packageName}.service;

import ${packageName}.${suffixModelPackageName}.${classNameModel};

/**
 * Manage the security business logic for this application.
 * 
 * @author Mesofi
 * @since ${date}
 */
public interface ${className} {
    
    public boolean authUser(${classNameModel} ${classNameModel.substring(0,1).toLowerCase()}${classNameModel.substring(1)});
    
}